def compare_shadow_records(shadow_records: list[dict]) -> dict:
    # TODO: raise ValueError if shadow_records is empty
    # TODO: count records where blue_label == green_label
    # TODO: compute abs(green_probability - blue_probability) per record
    # TODO: return a dict with:
    #       comparison_count,
    #       label_agreement_rate (4 decimals),
    #       disagreement_count,
    #       mean_probability_delta (4 decimals),
    #       max_probability_delta (4 decimals)
    raise NotImplementedError
